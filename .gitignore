node_modules/
.next/
out/
dist/
build/
coverage/
.turbo/
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
.coverage.*
coverage.xml
.env
.env.*
!.env.example
*.pem
*.key
!**/testdata/*.key
.DS_Store
.idea/
.vscode/
*.log
*.tsbuildinfo
.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl
playwright-report/
test-results/
backend/mutants/

# Local-only JWT signing key, generated on first run. Never commit.
.dev-keys/

apps/web/node_modules/
# Superseded by pnpm-lock.yaml. The project's packageManager is pnpm; an npm
# lockfile alongside it drifts silently and builds a different dependency tree.
apps/web/package-lock.json

# An early revision of the ESLint config. `.eslintrc.js` supersedes it (JSON
# cannot carry the comments explaining why the file has to exist at all) and
# takes precedence anyway. Left on disk only because it cannot be removed here.
apps/web/.eslintrc.json
